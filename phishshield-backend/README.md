# PhishShield AI Backend

Spring Boot 3 / Java 21 Maven backend scaffold. Configure PostgreSQL through `DB_URL`, `DB_USERNAME`, and `DB_PASSWORD` before running.

Swagger UI will be available at `/api/swagger-ui.html` once the application is running.
