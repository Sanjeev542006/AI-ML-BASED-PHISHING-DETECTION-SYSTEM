import { ShieldAlert } from 'lucide-react'
import { Card, Badge } from './ui'
import type { ScanResult as Result } from '../services/mockApi'
export function ScanResult({result,type}:{result:(Result & Record<string,unknown>)|null;type:string}){if(!result)return null;return <Card className="result"><div className="result-main"><div className={`score ${result.level.toLowerCase()}`}><span>RISK SCORE</span><strong>{result.risk}<small>/100</small></strong></div><div><Badge level={result.level}>{result.level} RISK</Badge><h2>{type} analysis complete</h2><p>Confidence score: <b>{result.confidence}%</b> · Our detection engine found indicators requiring review.</p></div></div><div className="reasons"><h3>Detection signals</h3>{result.reasons.map(reason=><div key={reason}><ShieldAlert size={16}/>{reason}</div>)}</div>{'whois' in result&&<div className="detail-grid"><div><span>WHOIS SUMMARY</span><b>{String(result.whois)}</b></div><div><span>SSL STATUS</span><b>{String(result.ssl)}</b></div><div><span>REDIRECT COUNT</span><b>{String(result.redirects)}</b></div></div>}</Card>}
