import { useState, type ReactNode } from 'react'
import { Bell, ChevronDown, Menu, MoreHorizontal, Search, ShieldCheck, X } from 'lucide-react'
import { NavLink, useLocation } from 'react-router-dom'
import { navigation } from '../constants/navigation'

function Sidebar({ open, close }: { open:boolean; close:()=>void }) { return <><div className={`scrim ${open?'show':''}`} onClick={close}/><aside className={`sidebar ${open?'open':''}`}><div className="brand"><div className="brand-mark"><ShieldCheck size={22}/></div><span>PhishShield</span><small>AI</small><button className="mobile-close" onClick={close}><X/></button></div><div className="workspace"><span>WORKSPACE</span><button>Security Operations <ChevronDown size={14}/></button></div><nav>{navigation.map(([label,path,Icon])=><NavLink key={path} to={path} end={path==='/'} onClick={close}><Icon size={18}/><span>{label}</span>{label==='Threat Intelligence'&&<i>3</i>}</NavLink>)}</nav><div className="sidebar-bottom"><div className="user"><div className="avatar">SK</div><div><b>Sanjeev </b><span>Security Admin</span></div><MoreHorizontal size={18}/></div></div></aside></> }
export function AppLayout({ children }: { children:ReactNode }) { const [open,setOpen]=useState(false);const location=useLocation();const title=navigation.find(item=>item[1]===location.pathname)?.[0]??'Workspace';return <div className="app"><Sidebar open={open} close={()=>setOpen(false)}/><main><header><button className="menu" onClick={()=>setOpen(true)}><Menu/></button><div className="crumb">Security Operations <span>/</span><b>{title}</b></div><div className="header-actions"><button><Search size={19}/></button><button className="notification"><Bell size={19}/><i/></button><div className="header-avatar">SK</div></div></header><div className="content">{children}</div><footer>© 2026 PhishShield AI <span>•</span> AI-powered protection for a safer web</footer></main></div> }
